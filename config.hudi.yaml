github:
  owner: apache
  repo: hudi
  branches:
    - master
    - "0.15"
  branch_ref_map:
    "0.15": release-0.15.0
  token_env: GITHUB_TOKEN
  request_delay_seconds: 1.0
  page_delay_seconds: 2.0
  max_retries: 5
  backoff_multiplier: 2.0
  respect_rate_limit: true

local_repo:
  upstream_url: https://github.com/apache/hudi.git
  repo_dir: ./workspace/hudi/upstream
  worktree_dir: ./workspace/hudi/worktrees
  target_ref:
    label: "0.15"
    ref: origin/release-0.15.0
    worktree_suffix: "0.15"

codex:
  command: codex
  mode: exec
  timeout_seconds: 7200
  max_attempts_per_pr: 2
  result_file: output/codex_result.json
  reasoning_effort: medium

analysis:
  default_limit: 5
  stale_timeout_seconds: 7200

reports:
  output_dir: ./reports/hudi

storage:
  sqlite_path: ./workspace/hudi/backport_harness.sqlite3
